# Configuração Banco
spring:
  datasource:
    url: jdbc:h2:mem:rotinalize;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

# Configuração e-mail
  mail:
    host: smtp.gmail.com
    port: 587
    username: rotinalize1@gmail.com
    password: rssyeywlffohjmae
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true # É bom forçar o TLS para o Gmail
        # Debug: descomente a linha abaixo se precisar ver o log detalhado do envio
        # debug: true

# Configuração IA
langchain4j:
  open-ai:
    chat-model:
      api-key:
      model-name: gpt-5-nano
      log-requests: true
      log-responses: true

# Configuração segurança (JWT)
jwt:
  private:
    key: "classpath:app.key"
  public:
    key: "classpath:app.pub"